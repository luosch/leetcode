class Solution(object):
    def reverseWords(self, s):
        if len(s) == 0:
            return 
        items = s.split( )
    
        answer = 
        while items:
            item = items.pop()
            if item != :
                answer +=  %s % item

        return answer[1:]
